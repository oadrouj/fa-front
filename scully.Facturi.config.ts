import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "Facturi",
  outDir: './dist/static',
  staticPort: 4200,
  routes: {
  }
};